server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: git@github.com:mibackend/quiz-pro-react_config-service.git
          clone-on-start: true
          private-key: D:/ProgramFiles/Git/PuttyKey/id_rsa
          default-label: master
          search-paths: config  #配置文件所在根目录
  application:
    name: config-server

eureka:
  client:
    service-url:
      defaultZone=http://localhost:8761/eureka: